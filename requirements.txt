# --- web ---
flask>=3.0
python-dotenv>=1.0

# --- data / ML ---
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
joblib>=1.3

# --- NLP ---
# (nltk is NOT required -- preprocessing.py uses regex tokenisation so it
#  works on Devanagari, which nltk's punkt tokeniser does not handle well)

# --- YouTube ---
google-api-python-client>=2.100

# --- map generation (only needed to run generate_nepal_map.py) ---
folium>=0.15
shapely>=2.0

# REMOVED from the old file:
#   textblob      -- was never imported
#   fuzzywuzzy    -- replaced by char n-gram features, which handle
#                    romanisation variance natively and much faster

# --- news / RSS ---
feedparser>=6.0
